def get_rfid_uid() -> str:
    """
    TODO (README #2): Replace with Arduino serial read:
        import serial
        ser = serial.Serial('/dev/ttyUSB0', 9600)
        uid = ser.readline().decode().strip()
        return uid
    """
    return 'MOCK_UID_1234'  # Simulated for software prototype
